batch script for downloading
content from the Google Sheets "CMS" and
parsing to JSON 

To Configure the script, open settings.py. 

To run the script:

1) install dependencies with 
    pip install -r requirements.txt
2) Run with
    python main.py 

Tested with Python3.8
